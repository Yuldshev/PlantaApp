import SwiftUI

struct ProfileView: View {
  var body: some View {
    VStack {
      Text("ProfileView")
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
    .background(.blue)
  }
}

#Preview {
  ProfileView()
}
