import SwiftUI

struct SearchView: View {
  var body: some View {
    Text("SeachView")
      .frame(maxWidth: .infinity, maxHeight: .infinity)
      .background(.green)
  }
}

#Preview {
  SearchView()
}
